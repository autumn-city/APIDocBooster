[ContextWrapper.getBaseContext() (hyper-link)]:  If you need access to a Context from within another context, you use a ContextWrapper. 
[View.getContext() (hyper-link)]:  Returns the context the view is currently running in.  Usually the currently active Activity.
